# Dependensi untuk skrip di tools/ (generator file template dari koordinat).
# TIDAK diperlukan oleh web app — web app 100% client-side (lihat web/).
python-docx>=1.1
lxml>=5.0
Pillow>=10.0
numpy>=1.26
